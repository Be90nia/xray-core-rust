# Build
/target
**/*.rs.bk
*.pdb
/target-fv/
__pycache__/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Beads / Dolt files
.dolt/
*.db
.beads-credential-key
.beads/

# Graphify
graphify-out/

# codebase-memory (MCP 知识图谱 artifact, 本地生成不提交)
.codebase-memory/

# Coverage
coverage/
*.profraw
*.profdata

# Environment
.env
.env.local

# OMO internal
.omo/
# Local Pi runtime state
.atl/

# Beads / Dolt files (added by bd init)
.beads/proxieddb/
*.gate.lock*

# Cargo user-level config (本机性能调优如 target-cpu=native 不该污染跨机器发行)
# 2026-09-20 cleanup: .cargo/config.toml 从历史抹除后兜底,避免误跟踪
/.cargo/


# Local logs
*.log

# 压测 harness 本地产物（metrics.csv / summary.md，不入库）
stress-out*/
stress-tmp-debug/

# docker 镜像构建注入的已 patch boringssl（本地复制自宿主机 boringssl-fv，不入库）
.docker-boringssl/

# 本地节点凭据与产物(严禁入库)
vps测试连接.txt
dist/xray*.exe
dist/*.body
dist/*.err
dist/*.txt
dist/__pycache__/
dist/interop_run/

# 工作台交接文档(本地维护,不入库)
HANDOFF.md
docs/
# 例外: docs/guide-highrtt-sockopt.md 运维部署指南, git add -f 一次性入库随发行物分发(bd c35a); 已跟踪文件不受上述规则影响, 其余 docs/ 照旧忽略
